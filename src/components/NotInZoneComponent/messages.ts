/*
 * NotInZoneComponent Messages
 *
 * This contains all the text for the NotInZoneComponent Screen.
 */

export const scope = 'app.screen.NotInZoneComponent';

const messages = {
  header: {
    scope: `${scope}.header`,
    options: {
      defaultValue: 'This is the NotInZoneComponent Screen!',
    },
  },
};

export default messages;
