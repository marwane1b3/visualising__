// import { selectShoppingCardScreenDomain } from '../selectors';

describe('selectShoppingCardScreenDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
