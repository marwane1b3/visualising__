/*
 * ShoppingCardScreen Messages
 *
 * This contains all the text for the ShoppingCardScreen Screen.
 */

export const scope = 'app.screen.ShoppingCardScreen';

const messages = {
  header: {
    scope: `${scope}.header`,
    options: {
      defaultValue: 'This is the ShoppingCardScreen Screen!',
    },
  },
};

export default messages;
