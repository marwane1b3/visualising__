/*
 * PaymentScreen Messages
 *
 * This contains all the text for the PaymentScreen Screen.
 */

export const scope = 'app.screen.PaymentScreen';

const messages = {
  header: {
    scope: `${scope}.header`,
    options: {
      defaultValue: 'This is the PaymentScreen Screen!',
    },
  },
};

export default messages;