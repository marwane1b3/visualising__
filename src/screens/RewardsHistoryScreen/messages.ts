/*
 * RewardsHistoryScreen Messages
 *
 * This contains all the text for the RewardsHistoryScreen Screen.
 */

export const scope = 'app.screen.RewardsHistoryScreen';

const messages = {
  header: {
    scope: `${scope}.header`,
    options: {
      defaultValue: 'This is the RewardsHistoryScreen Screen!',
    },
  },
};

export default messages;
