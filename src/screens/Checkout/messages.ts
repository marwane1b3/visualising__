/*
 * Checkout Messages
 *
 * This contains all the text for the Checkout Screen.
 */

export const scope = 'app.screen.Checkout';

const messages = {
  header: {
    scope: `${scope}.header`,
    options: {
      defaultValue: 'This is the Checkout Screen!',
    },
  },
};

export default messages;