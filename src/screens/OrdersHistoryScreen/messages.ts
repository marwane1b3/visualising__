/*
 * OrdersHistoryScreen Messages
 *
 * This contains all the text for the OrdersHistoryScreen Screen.
 */

export const scope = 'app.screen.OrdersHistoryScreen';

const messages = {
  header: {
    scope: `${scope}.header`,
    options: {
      defaultValue: 'This is the OrdersHistoryScreen Screen!',
    },
  },
};

export default messages;
