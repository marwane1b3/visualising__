// import { selectOrdersHistoryScreenDomain } from '../selectors';

describe('selectOrdersHistoryScreenDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
