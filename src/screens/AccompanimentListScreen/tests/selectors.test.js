// import { selectAccompanimentListScreenDomain } from '../selectors';

describe('selectAccompanimentListScreenDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
