// import { selectItemsListScreenDomain } from '../selectors';

describe('selectItemsListScreenDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
