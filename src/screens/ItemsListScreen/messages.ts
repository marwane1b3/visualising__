/*
 * ItemsListScreen Messages
 *
 * This contains all the text for the ItemsListScreen Screen.
 */

export const scope = 'app.screen.ItemsListScreen';

const messages = {
  header: {
    scope: `${scope}.header`,
    options: {
      defaultValue: 'This is the ItemsListScreen Screen {{name}}!',
    },
  },
};

export default messages;
