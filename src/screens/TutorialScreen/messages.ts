/*
 * TutorialScreen Messages
 *
 * This contains all the text for the TutorialScreen Screen.
 */

export const scope = 'app.screen.TutorialScreen';

const messages = {
  header: {
    scope: `${scope}.header`,
    options: {
      defaultValue: 'This is the TutorialScreen Screen!',
    },
  },
};

export default messages;
