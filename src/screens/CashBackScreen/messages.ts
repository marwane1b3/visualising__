/*
 * CashBackScreen Messages
 *
 * This contains all the text for the CashBackScreen Screen.
 */

export const scope = 'app.screen.CashBackScreen';

const messages = {
  header: {
    scope: `${scope}.header`,
    options: {
      defaultValue: 'This is the CashBackScreen Screen!',
    },
  },
};

export default messages;
