/*
 * ReferralScreen Messages
 *
 * This contains all the text for the ReferralScreen Screen.
 */

export const scope = 'app.screen.ReferralScreen';

const messages = {
  header: {
    scope: `${scope}.header`,
    options: {
      defaultValue: 'This is the ReferralScreen Screen!',
    },
  },
};

export default messages;
