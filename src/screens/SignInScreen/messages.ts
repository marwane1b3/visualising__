/*
 * SignInScreen Messages
 *
 * This contains all the text for the SignInScreen Screen.
 */

export const scope = 'app.screen.SignInScreen';

const messages = {
  header: {
    scope: `${scope}.header`,
    options: {
      defaultValue: 'This is the SignInScreen Screen!',
    },
  },
};

export default messages;
