// import { selectSignInScreenDomain } from '../selectors';

describe('selectSignInScreenDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
