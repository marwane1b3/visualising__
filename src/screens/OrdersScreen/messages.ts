/*
 * OrdersScreen Messages
 *
 * This contains all the text for the OrdersScreen Screen.
 */

export const scope = 'app.screen.OrdersScreen';

const messages = {
  header: {
    scope: `${scope}.header`,
    options: {
      defaultValue: 'This is the OrdersScreen Screen!',
    },
  },
};

export default messages;
