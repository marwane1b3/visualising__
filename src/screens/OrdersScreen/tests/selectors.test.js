// import { selectOrdersScreenDomain } from '../selectors';

describe('selectOrdersScreenDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
