// import { selectMycardsScreenDomain } from '../selectors';

describe('selectMycardsScreenDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
