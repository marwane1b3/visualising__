// import { selectForgotPasswordScreenDomain } from '../selectors';

describe('selectForgotPasswordScreenDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
