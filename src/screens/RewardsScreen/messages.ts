/*
 * RewardsScreen Messages
 *
 * This contains all the text for the RewardsScreen Screen.
 */

export const scope = 'app.screen.RewardsScreen';

const messages = {
  header: {
    scope: `${scope}.header`,
    options: {
      defaultValue: 'This is the RewardsScreen Screen!',
    },
  },
};

export default messages;
