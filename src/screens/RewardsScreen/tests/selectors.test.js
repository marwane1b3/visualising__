// import { selectRewardsScreenDomain } from '../selectors';

describe('selectRewardsScreenDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
