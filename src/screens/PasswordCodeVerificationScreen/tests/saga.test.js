/**
 * Test sagas
 */

/* eslint-disable redux-saga/yield-effects */
// import { take, call, put, select } from 'redux-saga/effects';
// import passwordCodeVerificationScreenSaga from '../saga';

// const generator = passwordCodeVerificationScreenSaga();

describe('passwordCodeVerificationScreenSaga Saga', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
