// import { selectPasswordCodeVerificationScreenDomain } from '../selectors';

describe('selectPasswordCodeVerificationScreenDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
