/*
 * PasswordCodeVerificationScreen Messages
 *
 * This contains all the text for the PasswordCodeVerificationScreen Screen.
 */

export const scope = 'app.screen.PasswordCodeVerificationScreen';

const messages = {
  header: {
    scope: `${scope}.header`,
    options: {
      defaultValue: 'This is the PasswordCodeVerificationScreen Screen!',
    },
  },
};

export default messages;
