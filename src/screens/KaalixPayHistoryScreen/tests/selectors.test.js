// import { selectKaalixPayHistoryScreenDomain } from '../selectors';

describe('selectKaalixPayHistoryScreenDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
