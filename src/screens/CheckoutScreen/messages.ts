/*
 * CheckoutScreen Messages
 *
 * This contains all the text for the CheckoutScreen Screen.
 */

export const scope = 'app.screen.CheckoutScreen';

const messages = {
  header: {
    scope: `${scope}.header`,
    options: {
      defaultValue: 'This is the CheckoutScreen Screen!',
    },
  },
};

export default messages;
