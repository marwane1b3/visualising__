// import { selectWinPointsScreenDomain } from '../selectors';

describe('selectWinPointsScreenDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
