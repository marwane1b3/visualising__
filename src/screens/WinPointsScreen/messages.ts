/*
 * WinPointsScreen Messages
 *
 * This contains all the text for the WinPointsScreen Screen.
 */

export const scope = 'app.screen.WinPointsScreen';

const messages = {
  header: {
    scope: `${scope}.header`,
    options: {
      defaultValue: 'This is the WinPointsScreen Screen!',
    },
  },
};

export default messages;
