/*
 * AddCardScreen Messages
 *
 * This contains all the text for the AddCardScreen Screen.
 */

export const scope = 'app.screen.AddCardScreen';

const messages = {
  header: {
    scope: `${scope}.header`,
    options: {
      defaultValue: 'This is the AddCardScreen Screen!',
    },
  },
};

export default messages;