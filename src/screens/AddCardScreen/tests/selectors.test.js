// import { selectAddCardScreenDomain } from '../selectors';

describe('selectAddCardScreenDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
