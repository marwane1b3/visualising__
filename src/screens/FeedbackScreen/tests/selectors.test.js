// import { selectFeedbackScreenDomain } from '../selectors';

describe('selectFeedbackScreenDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
