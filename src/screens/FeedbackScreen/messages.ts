/*
 * FeedbackScreen Messages
 *
 * This contains all the text for the FeedbackScreen Screen.
 */

export const scope = 'app.screen.FeedbackScreen';

const messages = {
  header: {
    scope: `${scope}.header`,
    options: {
      defaultValue: 'This is the FeedbackScreen Screen!',
    },
  },
};

export default messages;
