/*
 * KaalixPayScreen Messages
 *
 * This contains all the text for the KaalixPayScreen Screen.
 */

export const scope = 'app.screen.KaalixPayScreen';

const messages = {
  header: {
    scope: `${scope}.header`,
    options: {
      defaultValue: 'This is the KaalixPayScreen Screen!',
    },
  },
};

export default messages;
