/*
 * KaalixWinPointsScreen Messages
 *
 * This contains all the text for the KaalixWinPointsScreen Screen.
 */

export const scope = 'app.screen.KaalixWinPointsScreen';

const messages = {
  header: {
    scope: `${scope}.header`,
    options: {
      defaultValue: 'This is the KaalixWinPointsScreen Screen!',
    },
  },
};

export default messages;
