// import { selectAddressesListScreenDomain } from '../selectors';

describe('selectAddressesListScreenDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
