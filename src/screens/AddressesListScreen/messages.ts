/*
 * AddressesListScreen Messages
 *
 * This contains all the text for the AddressesListScreen Screen.
 */

export const scope = 'app.screen.AddressesListScreen';

const messages = {
  header: {
    scope: `${scope}.header`,
    options: {
      defaultValue: 'This is the AddressesListScreen Screen!',
    },
  },
  title: {
    scope: `${scope}.title`,
    options: {
      defaultValue: 'Adresses enregistées',
    },
  },
};

export default messages;
