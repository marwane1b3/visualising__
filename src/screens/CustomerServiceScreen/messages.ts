/*
 * CustomerServiceScreen Messages
 *
 * This contains all the text for the CustomerServiceScreen Screen.
 */

export const scope = 'app.screen.CustomerServiceScreen';

const messages = {
  header: {
    scope: `${scope}.header`,
    options: {
      defaultValue: 'This is the CustomerServiceScreen Screen!',
    },
  },
};

export default messages;
