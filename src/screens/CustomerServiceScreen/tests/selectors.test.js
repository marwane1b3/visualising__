// import { selectCustomerServiceScreenDomain } from '../selectors';

describe('selectCustomerServiceScreenDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
