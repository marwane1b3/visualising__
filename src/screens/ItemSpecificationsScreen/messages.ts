/*
 * ItemSpecificationsScreen Messages
 *
 * This contains all the text for the ItemSpecificationsScreen Screen.
 */

export const scope = 'app.screen.ItemSpecificationsScreen';

const messages = {
  header: {
    scope: `${scope}.header`,
    options: {
      defaultValue: 'This is the ItemSpecificationsScreen Screen!',
    },
  },
};

export default messages;
