// import { selectCheckoutPaymentScreenDomain } from '../selectors';

describe('selectCheckoutPaymentScreenDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
