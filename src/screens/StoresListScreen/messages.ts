/*
 * StoresListScreen Messages
 *
 * This contains all the text for the StoresListScreen Screen.
 */

export const scope = 'app.screen.StoresListScreen';

const messages = {
  header: {
    scope: `${scope}.header`,
    options: {
      defaultValue: 'This is the StoresListScreen Screen!',
    },
  },
};

export default messages;
