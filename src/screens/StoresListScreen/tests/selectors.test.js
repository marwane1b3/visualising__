// import { selectStoresListScreenDomain } from '../selectors';

describe('selectStoresListScreenDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
