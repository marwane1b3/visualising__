// import { selectProductsListScreenDomain } from '../selectors';

describe('selectProductsListScreenDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
