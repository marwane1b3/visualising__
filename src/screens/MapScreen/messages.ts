/*
 * MapScreen Messages
 *
 * This contains all the text for the MapScreen Screen.
 */

export const scope = 'app.screen.MapScreen';

const messages = {
  header: {
    scope: `${scope}.header`,
    options: {
      defaultValue: 'This is the MapScreen Screen!',
    },
  },
};

export default messages;
