/*
 * RegisterScreen Messages
 *
 * This contains all the text for the RegisterScreen Screen.
 */

export const scope = 'app.screen.RegisterScreen';

const messages = {
  header: {
    scope: `${scope}.header`,
    options: {
      defaultValue: 'This is the RegisterScreen Screen!',
    },
  },
};

export default messages;
