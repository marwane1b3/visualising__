// import { selectCardManagerScreenDomain } from '../selectors';

describe('selectCardManagerScreenDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
