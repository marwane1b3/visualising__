/*
 * OrderDetailsScreen Messages
 *
 * This contains all the text for the OrderDetailsScreen Screen.
 */

export const scope = 'app.screen.OrderDetailsScreen';

const messages = {
  header: {
    scope: `${scope}.header`,
    options: {
      defaultValue: 'This is the OrderDetailsScreen Screen!',
    },
  },
};

export default messages;
