// import { selectOrderDetailsScreenDomain } from '../selectors';

describe('selectOrderDetailsScreenDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
