/*
 *
 * AuthProvider constants
 *
 */

const scope = 'app/providers/AuthProvider';

export const SET_TOKEN = `${scope}/SET_TOKEN`;
export const REHYDRATE_TOKEN = `${scope}/REHYDRATE_TOKEN`;

export const STORE_TOKEN = `${scope}/STORE_TOKEN`;
export const DELETE_TOKEN = `${scope}/DELETE_TOKEN`;

export const PERSIST_TOKEN = `${scope}/PERSIST_TOKEN`;
