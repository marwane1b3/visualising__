// import { selectAdBannierContainerDomain } from '../selectors';

describe('selectAdBannierContainerDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
