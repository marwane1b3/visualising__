// import { selectReclamationContainerDomain } from '../selectors';

describe('selectReclamationContainerDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
