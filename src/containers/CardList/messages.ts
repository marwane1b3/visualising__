/*
 * CardList Messages
 *
 * This contains all the text for the CardList container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.CardList';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the CardList container!',
  },
});
