/*
 *
 * RegisterForm reducer
 *
 */

import produce from 'immer';

export const initialState = {};

/* eslint-disable default-case, no-param-reassign */
const registerFormReducer = produce((draft, action) => {
  switch (action.type) {
  }
}, initialState);

export default registerFormReducer;
