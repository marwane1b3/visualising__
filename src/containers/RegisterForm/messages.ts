/*
 * RegisterForm Messages
 *
 * This contains all the text for the RegisterForm Screen.
 */

export const scope = 'app.screen.RegisterForm';

const messages = {
  header: {
    scope: `${scope}.header`,
    options: {
      defaultValue: 'This is the RegisterForm Screen!',
    },
  },
};

export default messages;
