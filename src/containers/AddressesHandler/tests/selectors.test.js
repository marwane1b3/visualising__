// import { selectAddressesHandlerDomain } from '../selectors';

describe('selectAddressesHandlerDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
