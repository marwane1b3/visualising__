/*
 * LoginForm Messages
 *
 * This contains all the text for the LoginForm Screen.
 */

export const scope = 'app.screen.LoginForm';

const messages = {
  header: {
    scope: `${scope}.header`,
    options: {
      defaultValue: 'This is the LoginForm Screen!',
    },
  },
};

export default messages;
